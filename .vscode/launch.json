{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run PHPUnit Tests",
            "type": "php",
            "request": "launch",
            "program": "${workspaceFolder}/vendor/bin/phpunit",
            "cwd": "${workspaceFolder}",
            "args": [
                "--configuration",
                "${workspaceFolder}/phpunit.xml"
            ]
        }
    ]
}
